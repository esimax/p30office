<dx:DXWindow
    x:Class="POC.Module.Email.Views.WTemplateAddEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    Width="620"
    Height="520" 
    
    Title="{Binding WindowTitle}"
    Icon="{DynamicResource ApplicationIcon}"
    
    xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize" 
    
    FontFamily="{DynamicResource ApplicationFontFamily}" 
    FontSize="{DynamicResource ApplicationFontSize}"
    >


    <Grid pol_ap:ApGrid.RowDefinitions="auto,auto,*,auto,*,auto" 
          pol_ap:ApGrid.ColumnDefinitions="auto,*" Margin="9" 
          FlowDirection="{DynamicResource ApplicationFlowDirection}">

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="عنوان :" />
        <dxe:TextEdit Grid.Row="0" Grid.Column="1" Margin="3" 
                      EditValue="{Binding Title,Mode=TwoWay}" MaxLength="128"  Name="firstFocused"/>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Margin="3" VerticalAlignment="Center" Text="متن HTML قالب :" />
        <dxe:TextEdit Grid.Row="2" Grid.ColumnSpan="2" Margin="3" 
                      EditValue="{Binding TemplateBody,Mode=TwoWay}" 
                      FlowDirection="LeftToRight" ScrollViewer.CanContentScroll="True"
                      HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible"
                      VerticalContentAlignment="Top" AcceptsReturn="True" AcceptsTab="True"
                      />
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal">
            <TextBlock Margin="3" VerticalAlignment="Center" Text="متغییر ها :"/>
            <Button Grid.Row="0" Grid.Column="2" Padding="2" Margin="3" Command="{Binding CommandParameterAdd}">
                <Image Width="16" Height="16" Source="pack://application:,,,/POL.Lib.Resources;component/Standard/16/_16_Add.png"/>
            </Button>
            <Button Grid.Row="0" Grid.Column="3" Padding="2" Margin="3" Command="{Binding CommandParameterEdit}">
                <Image Width="16" Height="16" Source="pack://application:,,,/POL.Lib.Resources;component/Standard/16/_16_EditBlue.png"/>
            </Button>
            <Button Grid.Row="0" Grid.Column="4" Padding="2" Margin="3" Command="{Binding CommandParameterDelete}">
                <Image Width="16" Height="16" Source="pack://application:,,,/POL.Lib.Resources;component/Standard/16/_16_Delete2.png"/>
            </Button>
        </StackPanel>

        <ListBox Margin="3" Grid.Row="4" Grid.ColumnSpan="2" ItemsSource="{Binding ParamList}" SelectedItem="{Binding SelectedParam}" >
            <!--<ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>-->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="3" Text="{Binding Path=Tag}" Width="120" />
                        <TextBlock Margin="3" Text="{Binding Path=TagTypeString}" Width="90" />
                        <TextBlock Margin="3" Text="{Binding Path=ProfileItemString}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>



        <StackPanel Grid.Row="1000" Grid.ColumnSpan="1000" Orientation="Horizontal" Margin="3" HorizontalAlignment="Right">
            <Button Margin="0" Padding="6,3" MinWidth="80" IsDefault="True"
                    Command="{Binding CommandOK}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageOK16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="تایید"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" IsCancel="True"
                    Command="{Binding CommandCancel}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageCancel16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="لغو"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" 
                    Command="{Binding CommandHelp}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageHelp16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="راهنما"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</dx:DXWindow>

