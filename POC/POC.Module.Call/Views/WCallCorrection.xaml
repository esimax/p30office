<dx:DXWindow
    x:Class="POC.Module.Call.Views.WCallCorrection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
     Width="440"
    Height="320" 
    
    Title="{Binding WindowTitle}"
    Icon="{DynamicResource ApplicationIcon}"
    
    xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize" 
    
    FontFamily="{DynamicResource ApplicationFontFamily}" 
    FontSize="{DynamicResource ApplicationFontSize}"
    >

    <Grid pol_ap:ApGrid.RowDefinitions="auto,auto,auto,auto,auto,auto,*,auto" 
          pol_ap:ApGrid.ColumnDefinitions="auto,*" Margin="9" 
          FlowDirection="{DynamicResource ApplicationFlowDirection}">
        
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="اطلاعات دریافتی از دستگاه :"/>
        <dxe:TextEdit Grid.Row="0" Grid.Column="1" Margin="3" IsReadOnly="True"
                EditValue="{Binding OriginalNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                FlowDirection="LeftToRight" pol_ap:ApUIElement.EnKeyboardLayout=""  />
        
        <Button Grid.Row="1" Grid.Column="1" Margin="3" Content="اصلاح اتوماتیك"  Command="{Binding CommandCorrect}"/>


        <TextBlock Grid.Row="2" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="كد كشور :"/>
        <dxe:TextEdit Grid.Row="2" Grid.Column="1" Margin="3" 
                      EditValue="{Binding CountryCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      MaxLength="4"
                      Name="firstFocused"
                      FlowDirection="LeftToRight" pol_ap:ApUIElement.EnKeyboardLayout=""
                      />
        
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="كد شهر :"/>
        <dxe:TextEdit Grid.Row="3" Grid.Column="1" Margin="3" 
                EditValue="{Binding CityCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                MaxLength="3"
                FlowDirection="LeftToRight" pol_ap:ApUIElement.EnKeyboardLayout=""
                />
        
        <TextBlock Grid.Row="4" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="شماره تماس :"/>
        <dxe:TextEdit Grid.Row="4" Grid.Column="1" Margin="3" 
                      EditValue="{Binding PhoneNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      MaxLength="12"
                      FlowDirection="LeftToRight" pol_ap:ApUIElement.EnKeyboardLayout=""
                      />
        
        <TextBlock Grid.Row="5" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="شماره های مازاد :"/>
        <dxe:TextEdit Grid.Row="5" Grid.Column="1" Margin="3" 
                      EditValue="{Binding Extra,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      MaxLength="16"
                      FlowDirection="LeftToRight" pol_ap:ApUIElement.EnKeyboardLayout=""
                      />

        <StackPanel Grid.Row="1000" Grid.ColumnSpan="1000" Orientation="Horizontal" Margin="3" HorizontalAlignment="Right">
            <Button Margin="0" Padding="6,3" MinWidth="80" IsDefault="True"
                    Command="{Binding CommandOK}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageOK16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="تایید"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" IsCancel="True"
                    Command="{Binding CommandCancel}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageCancel16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="لغو"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" 
                    Command="{Binding CommandHelp}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageHelp16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="راهنما"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</dx:DXWindow>
