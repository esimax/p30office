<dx:DXWindow x:Class="POC.Module.Attachment.Views.WEventUnitAddEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        Title="اطلاعات برنامه" 
        Width="600"
        Height="600" 
    
        
    
        xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" 
        xmlns:dxre="http://schemas.devexpress.com/winfx/2008/xaml/richedit"
             
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" 
        Icon="{DynamicResource ApplicationIcon}"
        FontFamily="{DynamicResource ApplicationFontFamily}" 
        FontSize="{DynamicResource ApplicationFontSize}"
        FlowDirection="{DynamicResource ApplicationFlowDirection}"
             >
    <Grid pol_ap:ApGrid.ColumnDefinitions="auto,*,32,auto,*" pol_ap:ApGrid.RowDefinitions="auto,*,auto" Margin="9">
        
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="3" VerticalAlignment="Center" Text="نام رخداد :"/>
        <dxe:ComboBoxEdit Grid.Row="0" Grid.Column="1" Margin="3" Name="cbeEvents"/>

        <TextBlock Grid.Row="0" Grid.Column="3" Margin="3" VerticalAlignment="Center" Text="اولویت :"/>
        <dxe:TextEdit Grid.Row="0" Grid.Column="4" Margin="3" Mask="000"  MaskType="Numeric" Name="tePriority" />

        <dxre:RichEditControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Margin="3" x:Name="richEdit" 
                                          ActiveViewType="Draft"
                                          Loaded="richEdit_Loaded"
                                          InitializeDocument="richEdit_InitializeDocument"
                                          ShowHoverMenu="False"
                                          PopupMenuShowing="richEdit_PopupMenuShowing"
                                          ShowBorder="False"
                                          FlowDirection="LeftToRight">
        </dxre:RichEditControl>

        <StackPanel Grid.Row="1000" Grid.ColumnSpan="1000" Orientation="Horizontal" Margin="3" HorizontalAlignment="Right">
            <!--<Button Margin="0" Padding="6,3" MinWidth="80" IsDefault="True" Name="bCompile" Click="BCompile_OnClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="تحلیل"/>
                </StackPanel>
            </Button>-->
            <Button Margin="0" Padding="6,3" MinWidth="80" IsDefault="True" Click="ButtonBase_OnClick">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageSave16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="ثبت"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" IsCancel="True">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageCancel16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="لغو"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</dx:DXWindow>
