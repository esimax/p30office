<UserControl x:Class="POC.Module.BaseTable.Views.UBaseTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ItemsControl ItemsSource="{Binding BaseTableList}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Margin="3" ToolTip="{Binding Tooltip}" Command="{Binding Command}">
                    <StackPanel Orientation="Horizontal">
                        <Image Margin="3" Width="32" Height="32" Stretch="Fill" Source="{Binding Image}"/>
                        <TextBlock Margin="3" VerticalAlignment="Center" Text="{Binding Title}"/>
                      </StackPanel>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
