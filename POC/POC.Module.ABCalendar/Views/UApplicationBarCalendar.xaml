<UserControl xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"  xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"  x:Class="POC.Module.ABCalendar.Views.UApplicationBarCalendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             FlowDirection="{DynamicResource ResourceKey=ApplicationFlowDirection}"
             FontFamily="{DynamicResource ResourceKey=ApplicationFontFamily}"
             FontSize="{DynamicResource ApplicationFontSize}"
             
             xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
             xmlns:POLCalendarGadget="clr-namespace:POL.WPF.DXControls.POLCalendarGadget;assembly=POL.WPF.DXControls"
             xmlns:POLDateEdit="clr-namespace:POL.WPF.DXControls.POLDateEdit;assembly=POL.WPF.DXControls"
             xmlns:POLClock="clr-namespace:POL.WPF.DXControls.POLClock;assembly=POL.WPF.DXControls"
             
             mc:Ignorable="d" 
             d:DesignHeight="1600" d:DesignWidth="300">
    <Grid pol_ap:ApGrid.RowDefinitions="auto,*">

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="6">
            <Image Style="{DynamicResource ImageAny32}" Source="pack://application:,,,/POL.Lib.Resources;component/Standard/32/_32_calendar.png"/>
            <TextBlock Margin="3" VerticalAlignment="Center" Text="تقویم" FontFamily="{DynamicResource ApplicationFontFamilyH2}" FontSize="{DynamicResource ApplicationFontSizeH2}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Style="{DynamicResource BlendScrollViewerStyle}">
            <dxlc:FlowLayoutControl 
                                Name="flcMain"
                                AllowItemMoving="True" 
                                AllowAddFlowBreaksDuringItemMoving="False" 
                                AllowLayerSizing="False" 
                                AnimateItemMoving="True"
                                Orientation="Vertical" 
                                BreakFlowToFit="False" 
                                StretchContent="True" 
                                AllowMaximizedElementMoving="True" MaximizedElementPosition="Right"
                                ScrollBars="None"
                                >


                <Border Name="bTime" BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Margin="3" Background="White">
                    <Grid pol_ap:ApGrid.ColumnDefinitions="*" pol_ap:ApGrid.RowDefinitions="*,auto" Margin="6" >
                        <POLClock:POLClockAnalog Grid.Row="0" Grid.RowSpan="1" Grid.Column="0" Width="120" Height="120" FlowDirection="LeftToRight"/>
                        <!--<TextBlock Grid.Row="1" Margin="3" Text="{Binding CurrentTime}" FontFamily="pack://application:,,,/POL.Lib.Resources;component/Fonts/#Digital-7" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="6"/>-->
                        <TextBlock Grid.Row="1" Margin="3" Text="{Binding CurrentTime}" FontFamily="../#Digital-7" FlowDirection="LeftToRight" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="6" FontStyle="Italic" FontWeight="Bold"/>
                    </Grid>
                </Border>

                <Border Name="bNow" BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Margin="3" Background="{Binding DayOffsetBrush}">
                    <TextBlock Margin="3" Text="{Binding DayOffsetText}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="6"/>
                </Border>

                <POLCalendarGadget:POLCalendarGadget Name="cgShamsi" Foreground="White" Height="78" Margin="3" CalendarType="Shamsi" FillBrush="#FC6500" DateEditValue="{Binding SelectedDate}"/>
                <POLCalendarGadget:POLCalendarGadget Name="cgHijri" Foreground="White" Height="78" Margin="3" CalendarType="Hijri" FillBrush="#00B001" DateEditValue="{Binding SelectedDate}" HijriOffset="{Binding HijriOffset}"/>
                <POLCalendarGadget:POLCalendarGadget Name="cgGregorian" Foreground="White" Height="78" Margin="3" CalendarType="Gregorian" FillBrush="#2200CA" DateEditValue="{Binding SelectedDate}"/>

                <!--<dxlc:GroupBox Name="gbBrowse" Header="مرور تقویم" Margin="3">
                    <Grid pol_ap:ApGrid.RowDefinitions="auto,auto,auto" pol_ap:ApGrid.ColumnDefinitions="*,*,*,*">
                        <POLDateEdit:POLDateEdit Grid.Row="0"  Grid.ColumnSpan="4" Margin="3" DateEditValue="{Binding SelectedDate, Mode=TwoWay}" AllowEmptyDate="False" FlowDirection="LeftToRight" />

                        <Button Grid.Row="1" Grid.Column="0" Command="{Binding CommandPrevDay}" Margin="3" Padding="3" ToolTip="برو به یك روز قبل">
                            « روز
                        </Button>
                        <Button Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Command="{Binding CommandToday}" Margin="3" Padding="3" ToolTip="امروز">
                            امروز
                        </Button>
                        <Button Grid.Row="1"  Grid.Column="3" Command="{Binding CommandNextDay}" Margin="3" Padding="3" ToolTip="یك روز بعد">
                            روز »
                        </Button>


                        <Button Grid.Row="2" Grid.Column="0" Command="{Binding CommandPrevMonth}" Margin="3" Padding="3" ToolTip="یك ماه قبل">
                            « ماه
                        </Button>
                        <Button Grid.Row="2" Grid.Column="1" Command="{Binding CommandPrevWeek}" Margin="3" Padding="3" ToolTip="یك هفته قیل">
                            « هفته
                        </Button>

                        <Button Grid.Row="2" Grid.Column="2" Command="{Binding CommandNextWeek}" Margin="3" Padding="3" ToolTip="یك هفته بعد">
                            هفته »
                        </Button>
                        <Button Grid.Row="2" Grid.Column="3" Command="{Binding CommandNextMonth}" Margin="3" Padding="3" ToolTip="یك ماه بعد">
                            ماه »
                        </Button>
                    </Grid>
                </dxlc:GroupBox>-->
                <dxlc:GroupBox Name="gbBrowse" Header="مرور تقویم" Margin="3" Padding="0">
                    <POLDateEdit:MyDateEditCalendar Name="mydate"  ShowClearButton="False" Margin="0"  />
                </dxlc:GroupBox>

                <dxlc:GroupBox Name="gbEvents" Header="مناسبات" Margin="3" Height="120" Padding="0">
                    <dxe:ListBoxEdit ItemsSource="{Binding CalendarEventList}" ShowCustomItems="False" Margin="0" BorderBrush="Transparent" BorderThickness="0" />
                </dxlc:GroupBox>

                <dxlc:GroupBox Name="gbPray" Header="اوقات شرعی" Margin="3">
                    <Grid pol_ap:ApGrid.RowDefinitions="auto,auto,auto,auto,auto,auto,auto" pol_ap:ApGrid.ColumnDefinitions="auto,auto,*">
                        <TextBlock Grid.Row="0" Grid.Column="0"  Margin="3" Text="فجر"/>
                        <TextBlock Grid.Row="0" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeFajr}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0"  Margin="3" Text="طلوع آفتاب"/>
                        <TextBlock Grid.Row="1" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeSunrise}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0"  Margin="3" Text="ظهر"/>
                        <TextBlock Grid.Row="2" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeDhuhr}"/>

                        <TextBlock Grid.Row="3" Grid.Column="0"  Margin="3" Text="عصر"/>
                        <TextBlock Grid.Row="3" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeAsr}"/>

                        <TextBlock Grid.Row="4" Grid.Column="0"  Margin="3" Text="غروب خورشید"/>
                        <TextBlock Grid.Row="4" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeSunset}"/>

                        <TextBlock Grid.Row="5" Grid.Column="0"  Margin="3" Text="اذان مغرب"/>
                        <TextBlock Grid.Row="5" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeMaghrib}"/>

                        <TextBlock Grid.Row="6" Grid.Column="0"  Margin="3" Text="عشا"/>
                        <TextBlock Grid.Row="6" Grid.Column="1"  Margin="3" Text="{Binding PrayTimeIsha}"/>
                    </Grid>
                </dxlc:GroupBox>

                <Button Margin="3" Command="{Binding CommandHelp}">راهنما</Button>
            </dxlc:FlowLayoutControl>
        </ScrollViewer>
    </Grid>
</UserControl>
