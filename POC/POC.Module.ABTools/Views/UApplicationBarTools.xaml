<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Class="POC.Module.ABTools.Views.UApplicationBarTools" 
             
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="300"
             FlowDirection="{DynamicResource ResourceKey=ApplicationFlowDirection}"
             FontFamily="{DynamicResource ResourceKey=ApplicationFontFamily}"
             FontSize="{DynamicResource ApplicationFontSize}"
            
             xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" 
    >
    <Grid pol_ap:ApGrid.RowDefinitions="auto,*">

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="6">
            <Image Style="{DynamicResource ImageAny32}" Source="pack://application:,,,/POL.Lib.Resources;component/Standard/32/_32_Settings.png"/>
            <TextBlock Margin="3" VerticalAlignment="Center" Text="خانه" FontFamily="{DynamicResource ApplicationFontFamilyH2}" FontSize="{DynamicResource ApplicationFontSizeH2}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Style="{DynamicResource BlendScrollViewerStyle}">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding RootToolList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Margin="3" Padding="0" ToolTip="{Binding Tooltip}" HorizontalContentAlignment="Left" Command="{Binding Command}">
                                <StackPanel Orientation="Horizontal">
                                    <Image Margin="3" Width="32" Height="32" Stretch="Fill" Source="{Binding Image}" VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                                    <TextBlock Margin="3" VerticalAlignment="Center" Text="{Binding Title}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>                    
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
