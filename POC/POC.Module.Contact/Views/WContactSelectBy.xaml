<dx:DXWindow
    x:Class="POC.Module.Contact.Views.WContactSelectBy"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    Width="340"
    Height="220" 
    
    Title="{Binding WindowTitle}"
    Icon="{DynamicResource ApplicationIcon}"
    
    xmlns:pol_ap="clr-namespace:POL.WPF.Controles.AttachProperties;assembly=POL.WPF.Controles"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize" 
    
    FontFamily="{DynamicResource ApplicationFontFamily}" 
    FontSize="{DynamicResource ApplicationFontSize}"
    >


    <Grid pol_ap:ApGrid.RowDefinitions="auto,auto,auto,*,auto" 
          pol_ap:ApGrid.ColumnDefinitions="auto,*" Margin="9" 
          FlowDirection="{DynamicResource ApplicationFlowDirection}">


        <RadioButton Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="3" VerticalAlignment="Center" Content="همه پرونده ها" Name="firstFocused" IsChecked="{Binding IsAll}"/>


        <RadioButton Grid.Row="1" Grid.Column="0" Margin="3" VerticalAlignment="Center" Content="بر حسب دسته :" Name="rbByCat" IsChecked="{Binding IsCategory}" />
        <dxe:ComboBoxEdit Grid.Row="1" Grid.Column="2" Margin="3" VerticalAlignment="Center" AllowSpinOnMouseWheel="False" 
                          IsTextEditable="False" ImmediatePopup="True"
                          IsEnabled="{Binding IsChecked,ElementName=rbByCat}"
                          ItemsSource="{Binding ContactCategoryList}"
                          SelectedItem="{Binding SelectedCategory}"
                          />

        <RadioButton Grid.Row="2" Grid.Column="0" Margin="3" VerticalAlignment="Center" Content="بر حسب سبد انتخاب :" Name="rbByBasket" IsChecked="{Binding IsBasket}"/>
        <dxe:ComboBoxEdit Grid.Row="2" Grid.Column="2" Margin="3" VerticalAlignment="Center"  AllowSpinOnMouseWheel="False"
                          IsTextEditable="False" ImmediatePopup="True"
                          IsEnabled="{Binding IsChecked,ElementName=rbByBasket}"
                          ItemsSource="{Binding ContactBasketList}"
                          SelectedItem="{Binding SelectedBasket}"
                          />




        <StackPanel Grid.Row="1000" Grid.ColumnSpan="1000" Orientation="Horizontal" Margin="3" HorizontalAlignment="Right">
            <Button Margin="0" Padding="6,3" MinWidth="80" IsDefault="True"
                    Command="{Binding CommandOK}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageOK16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="تایید"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" IsCancel="True"
                    Command="{Binding CommandCancel}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageCancel16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="لغو"/>
                </StackPanel>
            </Button>
            <Button Margin="12,0,0,0" Padding="6,3" MinWidth="80" 
                    Command="{Binding CommandHelp}">
                <StackPanel Orientation="Horizontal">
                    <Image Style="{DynamicResource ImageHelp16}" />
                    <TextBlock Margin="3" VerticalAlignment="Center" Text="راهنما"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</dx:DXWindow>